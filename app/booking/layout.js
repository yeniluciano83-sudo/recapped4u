export const metadata = {
  title: "Book Your Event — Recapped For You",
  description: "Tell us about your event and pick your editing style — takes less than 2 minutes.",
};

export default function BookingLayout({ children }) {
  return children;
}
