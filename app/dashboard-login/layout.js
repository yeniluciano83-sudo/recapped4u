export const metadata = {
  title: "Dashboard Login — Recapped For You",
  description: "Sign in to manage bookings and deliverables.",
  robots: { index: false, follow: false },
};

export default function DashboardLoginLayout({ children }) {
  return children;
}
